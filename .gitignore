node_modules
node_modules/
out/
dist/
*.log
.DS_Store
.vite/
*.tsbuildinfo

build/icon.png

.playwright-mcp/

# concurrent-session worktrees / local agent state
.claude/

# private backend lives in its own repo (eneskirca/nodeterm-server)
/nodebaseserver/

# local session handoff transcripts / app state
.nodeterm/

# NOTE: internal dev docs (docs/superpowers/ SDD plans+specs, docs/RELEASING.md) are
# TRACKED in this private repo so the full dev environment clones cleanly on a remote
# server. They are stripped from the PUBLIC mirror by publish.sh (PRIVATE_PATHS), not here.

# local publish helper — never exported to the public repo
/publish.sh
