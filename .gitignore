node_modules
node_modules/
out/
dist/
*.log
.DS_Store
.vite/
*.tsbuildinfo

build/icon.png

.playwright-mcp/

# concurrent-session worktrees / local agent state
.claude/

# private backend lives in its own repo (eneskirca/nodeterm-server)
/nodebaseserver/

# local session handoff transcripts / app state
.nodeterm/

# internal planning artifacts (SDD plans/specs) — not for the public repo
/docs/superpowers/
# release runbook (signing/notarization/Apple specifics) kept private
/docs/RELEASING.md

# local publish helper — never exported to the public repo
/publish.sh
